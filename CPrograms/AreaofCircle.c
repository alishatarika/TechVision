#include <stdio.h>

int main()
{

    int radius;
    

    printf("Enter value of radius: ");
    scanf("%d", &radius);

    
    printf("Area of Circle is %d", 3 * radius * radius);

    return 0;
}